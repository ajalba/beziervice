mod Point;
pub struct BezierCurve {
    points: Vec<Point>,
    grade: i8
}

impl BezierCurve {
    fn base_n_grade(grade: i32) -> String {
        !todo()
    }
    fn generate_curve(points: Vec<Point>) -> String {
        !todo()
    }
    fn evaluate(curve: BezierCurve, point: Point) -> Point {
        !todo()
    }
    fn generate_curves(points: Vec<Point>) -> Vec<BezierCurve> {
        !todo()
    }
    fn aprox_function(function: String, grade: i32) -> Vec<Point> {
        !todo()
    }

}